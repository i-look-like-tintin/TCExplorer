# Cache files - these are generated automatically and should not be committed
cache/
php/cache/dp4df/

# IBTrACS data files - CSV files are too large for GitHub, will be downloaded automatically
# But we keep the parsed JSON files for faster Azure deployment
*.csv
!php/cache/ibtracs/*.json

# Node modules (if any)
node_modules/

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Claude Code directory
.claude/

# Log files
*.log
error_log

# Temporary files
*.tmp
*.temp
.cache/

# Azure-specific files
*.azurewebsites.net
config.local.php
php/config.local.php

# Composer vendor directory (if dependencies are added later)
vendor/
composer.lock

# Azure deployment artifacts
.deployment.bak
